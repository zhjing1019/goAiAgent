# Go 依赖隔离（可选，一般由 scripts/go-env.sh 自动设置）
# 复制为 .go.env.local 可覆盖代理等配置（gitignore）

# 模块代理（国内推荐）
# GOPROXY=https://goproxy.cn,direct

# 私有模块不走代理（公司内网 Git 域名示例）
# GOPRIVATE=git.company.com,*.corp.example.com

# 关闭 checksum 校验（仅内网私服且明确需要时）
# GOSUMDB=off
